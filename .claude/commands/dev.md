---
description: Start the dev server and open in browser
---

Start the Next.js development server:

1. Run `pnpm dev` in the background
2. Wait for the server to be ready (watch for "Ready" in output)
3. Let the user know the dev server is running at http://localhost:3000
